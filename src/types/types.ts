export interface Book {
    id: number;
    name: string;
    author: string;
    available: boolean;
  }
  
  export interface Product {
    id: number;
    title: string;
    price: number;
    description: string;
    category: string;
    image: string;
  }
  